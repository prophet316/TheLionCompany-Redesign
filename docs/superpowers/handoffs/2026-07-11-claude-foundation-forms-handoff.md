# Claude Code handoff: The Gathering foundation and forms

You are the implementation owner for the foundation/content/SEO and forms/privacy/security phases of The Lion Company’s approved “The Gathering” rebuild.

Work in `/Users/jonathangibson/Antigravity/TheLionCompany` on `codex/the-gathering-rebuild`. Do not create a competing design, change the approved scope, push, deploy, promote, buy a service, alter DNS, or use Production provider resources. Preserve unrelated user changes. Commit each completed task separately with the commit message specified by its plan.

## Read before editing

Read these files completely, in order:

1. `docs/superpowers/specs/2026-07-11-the-gathering-website-design.md`
2. `docs/superpowers/plans/2026-07-11-the-gathering-rebuild.md`
3. `docs/superpowers/plans/2026-07-11-the-gathering-foundation-content-seo.md`
4. `docs/superpowers/plans/2026-07-11-the-gathering-forms-privacy-security.md`

Use the pinned versions, interfaces, tests, commands, evidence boundaries, and task order exactly. Write tests before implementation where the task says to do so. Never weaken a failing contract merely to make it green.

## Your sequential phases

Determine the next authorized Claude phase from current Git history and execute only that phase:

1. **Claude A — Foundation Tasks 1–8.** Start from planning commit `b1482dd`. Complete every foundation task in order, run its integrated handoff gate, and stop.
2. **Claude B — Forms Tasks 1–6.** Start only after Grok has completed Experience Tasks 1–6 on this branch. Complete Forms Tasks 1–6 and stop.
3. **Claude C — Forms Task 7.** Start only after Grok has completed Experience Tasks 7–8. Complete Forms Task 7 and stop.
4. **Claude D — Forms Task 8.** Start only after Grok has completed Experience Task 9. Complete Forms Task 8 and its final matrix, then stop.

If the preceding dependency is absent, do not improvise around it. Report the missing commit/phase and stop without editing.

## Non-negotiable product rules

- Giving is obvious but calm: permanent `Give` navigation, first-party `/give`, then the exact centralized Subsplash URL. No guilt, false urgency, countdown, timed giving prompt, or invented financial claim.
- Do not claim nonprofit/tax status, leadership titles, asset rights, retention facts, or provider behavior without the exact evidence required by the plans.
- Teaching posters are first-party and ledgered. Podcast rendering is a checked-in static snapshot only.
- Production form origins are exactly `https://www.thelioncompany.org`; Production Turnstile accepts exactly `www.thelioncompany.org`. Never add Preview or staged Vercel hosts.
- Prayer, contact, name, email, tokens, and provider content never enter logs, analytics, URLs, Git, release evidence, or site-owned persistence.
- No Production key may enter Preview, client code, source maps, logs, evidence, or Git.

## Human-only transcript gate

You may prepare the caption-derived transcript and the review tooling, but you may not self-attest the required authorized-human complete listen-through. Keep the teaching player ineligible until a human has reviewed all 2,223 seconds and the structured evidence record is identity/date/decision/SHA-256 bound to the exact corrected transcript. Do not invent a reviewer or mark the gate complete from word count, captions, or AI review. Continue every other safe Foundation task; report the exact human action still required.

## Completion report

At the end of each Claude phase, output only:

- phase completed;
- commits created;
- tests/checks run with pass/fail totals;
- any human or external-service gate still open;
- current clean/dirty Git status;
- the exact next authorized Grok or Claude phase.

Do not begin the next interleaved phase in the same run.
